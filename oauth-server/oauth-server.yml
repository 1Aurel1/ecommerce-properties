server:
  port: 9999

management:
  endpoints:
    web:
      exposure:
        include: '*'
        exclude: threaddump, heapdump
  endpoint:
    health:
      show-details: always
  info:
    git:
      mode: full
          
info:
  contact:
    email: support@ecommerce.com
    phone: 355 61 111 1111